# todoReduxtoolkit
TodoList Application is a full-stack task management app built using React.js for the frontend, Express.js for the backend, and MongoDB as the database. The application uses Redux Toolkit for state management and handles user authentication with JWT tokens. Users can sign up, log in, create todos, view them, and delete them securely.
